import { View, Text, TouchableOpacity } from 'react-native'
import React from 'react'

const RecetJobsCard = ({item}) => {
  return (
        <TouchableOpacity>
            
        </TouchableOpacity>
  )
}

export default RecetJobsCard;